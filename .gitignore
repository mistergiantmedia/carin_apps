.DS_Store
# Holds the webhook secret; this repo is public, so it lives only on the server
webhook/deploy.php
webhook/deploy.log

# Next.js source folders
node_modules/
.next/
.env.local

# Holds the DB password; lives only on the server
config.php

# Conversation images: only on the server, never in git
uploads/*
!uploads/index.html
